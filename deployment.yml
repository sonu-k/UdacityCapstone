apiVersion: apps/v1
kind: Deployment
metadata:
  name: udacitycapstonedeployment
  labels:
    app: udacitycapstone
spec:
  selector:
    matchLabels:
      app: udacitycapstone
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: udacitycapstone
    spec:
      containers:
      - name: udacitycapstone
        image: sonkumar0895/udacitycapstone
        imagePullPolicy: "Always"
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: udacitycapstonedeployment
  labels:
    app: udacitycapstonedeployment
spec:
  type: LoadBalancer
  selector:
    app: udacitycapstonedeployment
  ports:
    - port: 8000
      targetPort: 80
      protocol: TCP